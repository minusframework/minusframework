name: CI

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write
  pages: write
  id-token: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Cache Docusaurus build
        id: cache-docusaurus
        uses: actions/cache@v4
        with:
          path: |
            .docusaurus
            build
          key: docusaurus-${{ hashFiles('docusaurus.config.ts', 'sidebars.ts', 'src/**/*', 'docs/**/*', 'static/**/*') }}
          restore-keys: |
            docusaurus-
      - name: Build Docusaurus
        run: npm run build
      - name: Upload build artifact (PR preview)
        if: github.event_name == 'pull_request'
        uses: actions/upload-pages-artifact@v3
        with:
          path: build
      - name: Deploy to GitHub Pages
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build
          cname: minusframework.dev
          full_commit_message: "docs: deploy ${{ github.sha }}"
      - name: Healthcheck post-deploy
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          sleep 30
          for i in 1 2 3; do
            code=$(curl -s -o /dev/null -w "%{http_code}" "https://minusframework.github.io/minusframework/" 2>/dev/null || echo "000")
            if [ "$code" = "200" ]; then echo "Site is live"; exit 0; fi
            echo "Attempt $i: HTTP $code, retrying..."
            sleep 10
          done
          echo "Site not responding after 3 attempts"
          exit 1

  deploy-preview:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
      - run: npm ci
      - name: Build Docusaurus (preview baseUrl)
        run: BASE_URL=/minusframework/pr-${{ github.event.number }}/ npm run build
      - name: Deploy preview to GitHub Pages (PR)
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build
          destination_dir: pr-${{ github.event.number }}
      - name: Comment PR with preview link
        uses: actions/github-script@v7
        with:
          script: |
            const prNumber = context.issue.number;
            const previewUrl = `https://minusframework.github.io/minusframework/pr-${prNumber}/`;
            github.rest.issues.createComment({
              issue_number: prNumber,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `## Preview disponível\n\nAcesse: ${previewUrl}\n\n> Este preview é atualizado a cada push no PR.`
            });

  wiki:
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Deploy Wiki
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if (Test-Path "scripts/ci/deploy-wiki.ps1") {
            ./scripts/ci/deploy-wiki.ps1 -Token "$env:GH_TOKEN"
          }

  release-sync:
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Sync releases across submodules
        env:
          TOKEN: ${{ secrets.GH_PAT }}
        run: |
          $tag = "${{ github.ref_name }}"
          pwsh ./scripts/release/release-sync.ps1 -Version $tag -Token "$env:TOKEN"
