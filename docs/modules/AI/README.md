# MinusAI

**Agentes inteligentes e servidor MCP para o ecossistema MinusFrameWork.**

## Ferramentas MCP

| Ferramenta | DescriÃ§Ã£o |
|------------|-----------|
| `explicar_codigo` | Analisa um arquivo `.pas` e explica sua estrutura |
| `gerar_entidade` | Gera cÃ³digo ORM a partir de uma tabela |
| `criar_migracao` | Cria arquivo de migraÃ§Ã£o |
| `executar_consulta` | Executa SQL contra um banco |

## Uso

```powershell
MinusAI_MCP.exe
```

O servidor lÃª requisiÃ§Ãµes JSON-RPC 2.0 via **stdin** e responde via **stdout**.
